<!DOCTYPE html>
<html lang="en">

<head>
    <title><%= content_for(:title) || "EventPulse - Discover and Share Events" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="EventPulse - The ultimate platform for discovering and sharing events">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="EventPulse">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#6366f1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="alternate" type="application/rss+xml" title="EventPulse Feed" href="<%= feed_path(format: :rss) %>">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: #333;
        }

        .app-container {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .content-wrapper {
            flex: 1;
        }
    </style>
</head>

<body>
    <div class="min-h-screen flex flex-col bg-gray-100">
        <!-- Header -->
        <header class="bg-white shadow-sm border-b border-gray-200 sticky top-0 z-50">
            <nav class="max-w-full mx-auto px-8 flex justify-between items-center h-16">
                <div class="flex items-center gap-3">
                    <div class="w-10 h-10 bg-gradient-to-r from-indigo-600 to-purple-600 rounded-lg flex items-center justify-center shadow-md">
                        <span class="text-white font-bold text-lg">EP</span>
                    </div>
                    <%= link_to "EventPulse", root_path, class: "text-2xl font-bold text-gray-900 tracking-tight hover:text-indigo-600 transition" %>
                </div>
                <div class="flex items-center gap-6">
                    <%= link_to "All Posts", posts_path, class: "text-gray-600 hover:text-indigo-600 font-medium transition px-3 py-2 rounded-md hover:bg-gray-50" %>
                    <%= link_to feed_path(format: :rss), class: "inline-flex items-center gap-2 text-orange-600 hover:text-orange-700 font-medium transition px-3 py-2 rounded-md hover:bg-orange-50" do %>
                    <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                        <path d="M5.5 13a3.5 3.5 0 01-.369-6.98 4 4 0 117.753-1 4.5 4.5 0 1-3.762 5.98z"></path>
                    </svg>
                    <span>RSS Feed</span>
                    <% end %>
                </div>
            </nav>
        </header>

        <!-- Main Content -->
        <main class="flex-1 w-full">
            <div class="w-full flex justify-center items-center mx-auto px-4 sm:px-6 lg:px-8 py-12">
                <%= yield %>
            </div>
        </main>

        <!-- Footer -->
        <footer class="bg-gray-900 text-gray-400 py-12 border-t border-gray-800">
            <div class="w-full mx-auto px-4 sm:px-6 lg:px-8 text-center">
                <p class="text-lg font-semibold text-white mb-2">EventPulse</p>
                <p class="mb-6 text-sm">Discovering and sharing the best events since <%= Time.current.year %></p>
                <p class="text-sm text-gray-500">&copy; <%= Time.current.year %> EventPulse. All rights reserved.</p>

                <div class="mt-6">
                    <%= link_to "Admin", admin_posts_path, class: "inline-block bg-indigo-600 text-white px-3 py-1 rounded text-sm hover:bg-indigo-700 transition" %>
                </div>
            </div>
        </footer>
    </div>
</body>

</html>